struct Miner
{
    primes: Vec<u64>,
    primorialOffsets: Vec<Integer>

}


impl Miner
{
    fn generate_primetable(&self) -> Vec<u64>
    {

        return Vec::new();
    }

    fn get_prime(i: u64) -> u64
    {
        1
    }
}